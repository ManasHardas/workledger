# dependencies and builds
node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# local state (never the ledger; the ledger is committed)
.env
.env.*
!.env.example
*.log
.DS_Store

# editor and tool caches
.vscode/
.idea/
.superpowers/
coverage/

# agentwaves worktrees
.worktrees/

# Parked by packages/cli `prepack`, removed by `postpack` (scripts/bundle-cli.mjs).
packages/cli/package.json.prepack-backup

# Playwright output (apps/web/playwright.config.ts)
test-results/
playwright-report/
