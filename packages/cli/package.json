{
  "name": "workledger",
  "version": "0.0.1",
  "type": "module",
  "description": "Local observer for coding-agent sessions: checkpoint digests, ledger, backlog.",
  "license": "MIT",
  "bin": {
    "workledger": "./bin/workledger"
  },
  "files": ["dist/main.js", "dist/migrations", "bin"],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b && node ../../scripts/bundle-cli.mjs",
    "clean": "tsc -b --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^22",
    "@workledger/core": "workspace:*",
    "commander": "^15.0.0"
  }
}
